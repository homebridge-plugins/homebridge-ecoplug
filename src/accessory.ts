/**
 * EcoPlugAccessory — thin wrapper around a PlatformAccessory context.
 *
 * The bulk of the accessory logic lives in EcoPlugPlatform.  This file is
 * a placeholder for future per-accessory state that the platform delegates
 * down (e.g., brightness sliders for dimmable plugs).
 */

// Reserved for future per-accessory encapsulation.
export class EcoPlugAccessory {
    // Will hold per-device state in a future refactor.
}
